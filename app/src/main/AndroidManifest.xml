<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kosbooking">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".KHActivity"></activity>
        <activity android:name=".oderActivity.MyRoomActivity" />
        <activity android:name=".oderActivity.MyAirActivity" />
        <activity android:name=".ManagerOrderActivity" />
        <activity android:name=".oderActivity.OrderRoomActivity" />
        <activity android:name=".oderActivity.OrderAir1Activity" />
        <activity android:name=".InfActivity" />
        <activity android:name=".CreatedTicketActivity" />
        <activity android:name=".CreatedRoomActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".CreatedAccActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>